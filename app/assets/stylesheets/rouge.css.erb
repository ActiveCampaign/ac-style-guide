<%= Rouge::Themes::Github.render(:scope => '.highlight, code') %>
.highlight {
	padding: 0;
	background-color: #1F2129;
	color: #fff;
	margin-top: 20px;

	code {
		background-color: #1F2129;
	}

	.nt {
		color: #5884EA;
	}
	.na {
		color: #1BDDB1;
	}
	.s {
		color: #F3376B;
	}
}

.highlight > pre {
	overflow: auto;
	font-weight: normal;
	font-style: normal;
	font-size: 13px;
	padding: 10px;
}