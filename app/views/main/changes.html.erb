<header>
	<div class="display-wrap">
		<div class="grid g-cols">
			<div class="g-col-12">
				<h1 class="white">📝 What's New</h1>
			</div>
	</div>
</header>
<div class="display-wrap">
		<% @change_days.each do |day, changes| %>
		<div class="change--day">
			<p class="l--fs-6"><strong><%= day.strftime("%B %d, %Y") %></strong></p>
			<table class="style1">
			<% changes.each do |change| %>
			
				<tr>
					<td><%=raw change.description %></td>
				</tr>
			
			<% end %>
			</table>
		</div>
		<% end %>
		
</div>